@model almacenApp.Models.Producto

<h4>Producto</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form id="form" method="post" asp-controller="producto" asp-action="EditByID">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label  asp-for="id_producto" class="control-label"></label>
                <input  asp-for="id_producto" class="form-control" readonly/>
                <span  asp-validation-for="id_producto" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="nombre" class="control-label"></label>
                <input asp-for="nombre" class="form-control" />
                <span asp-validation-for="nombre" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="stock_cantidad" class="control-label"></label>
                <input asp-for="stock_cantidad" class="form-control" />
                <span asp-validation-for="stock_cantidad" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="categoria" class="control-label"></label>
                <input asp-for="categoria" class="form-control"/>
                <span asp-validation-for="categoria" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="precio" class="control-label"></label>
                <input asp-for="precio" class="form-control" />
                <span asp-validation-for="precio" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input id="btn" type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

<script>
    const regex = /^\d+$/;
    
    const form = document.getElementById('form');

    form.addEventListener((submit) =>  { 
    //todo
        //escuchar al cambio en input para precio
        //mostrar mensaje de error si no supera el test de regex
        /*
        if (!regex.test(label.value)) {
        // The value does not contain only numbers
        alert("Only numbers are allowed in this field.");
        }
        */ 
    });

</script>